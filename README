***************************************************************
* PHP5 Language parsing rules patch for Function List plugin  * 
* by Geoffray Warnants (http://geoffray.be)                   *
***************************************************************

INSTALLATION
------------
    - install Function List plugin if necessary
    - close Notepad++
    - unpack FunctionListRules.xml and php.bmp into your Notepad++'s "plugins\Config" folder
      but if you installed N++ with "Don't use %APPDATA%" unchecked, then unpack into "%APPDATA%\Notepad++\plugins" instead!
